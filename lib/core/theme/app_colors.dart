import 'package:flutter/material.dart';

// Soft Light Palette
const backgroundSoftLight = Color(0xFFFAFAFD);
const surfaceLightCard = Color(0xFFFFFFFF);
const primaryViolet = Color(0xFF8B5CF6);
const primaryPink = Color(0xFF814CC2);
const primaryLight = Color(0xFFA78BFA);
const secondaryTeal = Color(0xFF10B981);
const accentGold = Color(0xFFFBBF24);
const textPrimaryDark = Color(0xFF512A88);
const textSecondaryDark = Color(0xFF7B52B4);

// Editor specific colors
const sliderTrackLight = Color(0xFFE5E7EB);
const filterSelectedBorder = Color(0xFF814CC2);
const tabSelectedBackground = Color(0xFFF3E8FF);
const tabSelectedText = Color(0xFF814CC2);
const badgeRed = Color(0xFF814CC2);

// Save Button / Bottom Nav Gradient (vertical, top #CEBAFF -> bottom #CCF8FF)
const saveGradientColors = [Color(0xFFCEBAFF), Color(0xFFCCF8FF)];
const bottomNavGradientColors = [Color(0xFFCEBAFF), Color(0xFFCCF8FF)];

final bottomSheetGradientColors = [
  Colors.white.withValues(alpha: 0.85),
  Colors.white.withValues(alpha: 0.65),
  Colors.white.withValues(alpha: 0.80),
];

final glassBorderColors = [
  Colors.white.withValues(alpha: 0.95),
  Colors.white.withValues(alpha: 0.35),
];

final gelGlossColors = [
  Colors.white.withValues(alpha: 0.40),
  Colors.white.withValues(alpha: 0.10),
];

// Soft Gradient Background
const softBackgroundGradientColors = [
  Color(0xFFFAFAFD),
  Color(0xFFF3E8FF),
  Color(0xFFFCE7F3),
  Color(0xFFFAFAFD),
];

// Horizontal primary gradient
const primaryGradientColors = [primaryViolet, Color(0xFF814CC2)];
